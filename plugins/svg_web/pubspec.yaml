name: svg_web
version: 0.0.1

flutter:
  plugin:
    platforms:
      web:
        pluginClass: SvgWebPlugin
        fileName: svg_web.dart

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter

environment:
  sdk: '>=2.0.0-dev.28.0 <3.0.0'
